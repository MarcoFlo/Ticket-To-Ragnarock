<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.giuseppegarone.tickettoragnarok.LoseActivity"
    android:background="@color/sfondo">

    <TextView
        android:id="@+id/lose_activty_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="90dp"
        android:text="@string/sconfitta_titolo"
        android:textColor="@color/marrone"
        android:textSize="48sp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/lose_activty_title"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="100dp"
        android:layout_marginStart="100dp"
        android:layout_marginTop="90dp"
        android:gravity="center">

        <ImageButton
            android:id="@+id/try_again_btn"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:background="@color/trasparente"
            android:scaleType="centerInside"
            android:src="@drawable/tryagain_btn" />

        <ImageButton
            android:id="@+id/back_to_menu_btn"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:layout_marginLeft="24dp"
            android:layout_marginStart="24dp"
            android:background="@color/trasparente"
            android:scaleType="centerInside"
            android:src="@drawable/back_btn" />

    </LinearLayout>

</RelativeLayout>
